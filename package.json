{
  "name": "yara-online",
  "version": "0.0.0",
  "description": "Simple Yara rule database and file scanner",
  "main": "server.js",
  "author": "thisita",
  "dependencies": {
    "body-parser": "^1.4.3",
    "express": "^4.13.4",
    "express-session": "^1.15.1",
    "method-override": "^2.1.3",
    "mongoose": "^4.4.12",
    "morgan": "^1.1.1",
    "jade": "~1.11.0",
    "jquery": "^3.1.1",
    "bootstrap": "^3.3.7",
    "angular": "^1.6.2",
    "browserify": "^14.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-es2015-rollup": "^2.0.1",
    "coveralls": "^2.11.9",
    "eslint": "^3.16.1",
    "eslint-config-airbnb-base": "^11.1.0",
    "eslint-plugin-import": "^2.2.0",
    "istanbul": "^0.4.3",
    "mocha": "^3.1.2",
    "rollup": "^0.29.1",
    "uglify-js": "^2.6.2"
  },
  "scripts": {
    "lint": "eslint server.js models public",
    "clean:browser": "rm -f public/bundle.js",
    "clean": "npm run clean:browser",
    "pretest": "npm run lint",
    "test": "mocha --reporter spec",
    "coveralls": "istanbul cover _mocha --report lcovonly -x server.js -- -R spec && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "engines": {
    "node": ">= 0.10"
  }
}
